v-container.grid(fluid)
    ag-grid-vue.grid__wrap(
        class="ag-theme-alpine"
        suppressCellFocus
        :rowHeight="35"
        :columnDefs="columnDefs"
        :rowData="rowData"
        :frameworkComponents="frameworkComponents"
        :defaultColDef="defaultColDef"
        @grid-ready="onGridReady"
        @row-clicked="onRowClicked"
    )